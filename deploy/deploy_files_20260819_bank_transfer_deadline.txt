# FTP アップロード一覧（2026-08-19 hotfix）— 銀行振込の支払期限を翌月末に
# ローカル起点: deploy/webroot_billing/
# 本番先ルート: /var/www/vhosts/dschatbot.ai/httpdocs/webroot/billing/
#
# 前提確認（ローカル）:
# - app/ と deploy/webroot_billing/app/ の BillingScheduleService.php は IDENTICAL
# - DB変更なし / .env変更なし / build再ビルド不要
# - クレジットの発行・送付・決済期限は変更しない
# - 銀行振込のみ deadline を start_date 月の末日（0/99）にする

## A. 必須（1ファイルのみ）

app/app/Services/BillingRobo/BillingScheduleService.php

## B. アップロード禁止

# app/.env
# app/storage/
# build/
# app/database/migrations/
# vendor/

## C. デプロイ後

# PHPクラスのため views 削除は必須ではない
# 反映されない場合のみ Plesk で PHP/OPcache 再起動を検討
