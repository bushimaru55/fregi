# 本番環境用 .env テンプレート
# このファイルをコピーして deploy/webroot_billing/app/.env として配置し、実際の値を設定してください

APP_NAME="Billing System"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://dschatbot.ai/webroot/billing

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_COOKIE=billing_session
SESSION_PATH=/webroot/billing

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# 本番メール送信（dschatbot.ai SMTP・申込受付通知・送信テスト用）
MAIL_MAILER=smtp
MAIL_HOST=dschatbot.ai
MAIL_PORT=587
MAIL_USERNAME=admin-notify@dschatbot.ai
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=admin-notify@dschatbot.ai
MAIL_FROM_NAME="${APP_NAME}"
# 証明書検証エラーで送信できない場合のみ、以下を有効化（セキュリティ上、証明書を正しく設定することを推奨）
# MAIL_URL=smtp://admin-notify%40dschatbot.ai:パスワード@dschatbot.ai:587?verify_peer=0

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
